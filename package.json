{
  "name": "build-marathon",
  "version": "0.5.0",
  "private": true,
  "description": "BUILD interprets a marathon plan against completed training.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "start": "node dev-server.cjs",
    "test:browser": "node tests/browser.cjs",
    "build": "node scripts/build.cjs"
  },
  "devDependencies": {
    "playwright": "1.62.1"
  }
}
